# החייאת המערכת — אוגוסט 2026

## מה קרה

- **Vercel תקין**: הדיפלוי האחרון (main @ `b701f6a`) במצב READY ומגיש את האפליקציה.
- **Supabase מושהה**: פרויקט ה-CRM `fsbgxtmxvhxmmtcflmug` לא נפתר ב-DNS בכלל
  (`ENOTFOUND`) — התנהגות של פרויקט free-tier שהושהה אוטומטית אחרי שבוע ללא
  פעילות. הפעילות האחרונה הייתה באמצע יוני. בלי Supabase אין Auth ואין DB —
  ולכן אי-אפשר להתחבר למערכת.
- חיבור ה-MCP של Supabase בסשני Claude מצביע כרגע על חשבון אחר
  (megamodel2000@gmail.com) שלא מכיל את פרויקט ה-CRM, ולכן ההחייאה דורשת
  פעולה ידנית קצרה בדשבורד.

## צעדי החייאה (5 דקות)

1. **שחזור הפרויקט**: להתחבר לחשבון Supabase שבבעלותו הפרויקט ולפתוח
   <https://supabase.com/dashboard/project/fsbgxtmxvhxmmtcflmug> → ללחוץ
   **Restore project**. השחזור לוקח כמה דקות.
   - אם הפרויקט מסומן כמחוק (עוברים 90 יום מההשהיה בסביבות סוף ספטמבר) —
     יש לפעול מהר או לפתוח פנייה ל-Supabase.
2. **כולם אדמינים**: ב-SQL Editor של הפרויקט להריץ את
   `supabase/migrations/0012_everyone_admin.sql` (idempotent — בטוח להריץ
   שוב). זה הופך את כל המשתמשים הקיימים לאדמינים, וכל משתמש חדש יהיה אדמין
   אוטומטית.
3. **בדיקת הרכב המשתמשים**: להתחבר למערכת (yonilev2003@gmail.com הוא תמיד
   אדמין) → **ניהול משתמשים** (`/admin/users`):
   - לוודא שיוני, רוי ותומי מופיעים. מי שלא מופיע — פשוט מתחבר פעם אחת עם
     Google והפרופיל נוצר (אדמין אוטומטית אחרי המיגרציה).
   - אם ליה מופיעה — למחוק אותה בכפתור המחיקה. אם המחיקה נחסמת בגלל
     בעלות על משימות/הודעות — להעביר בעלות קודם, כפי שהממשק מציע.
4. **מניעת הישנות**: למזג את ה-branch הזה ל-main. הדיפלוי מוסיף
   `/api/health` + Vercel Cron יומי (06:00 UTC) שמריץ שאילתה אמיתית מול
   Postgres — כך הפרויקט לא ייחשב שוב כלא-פעיל ולא יושהה.
5. **מומלץ**: לחבר מחדש את מחבר ה-Supabase ב-claude.ai (Settings →
   Connectors) לחשבון שמחזיק את פרויקט ה-CRM, כדי שסשנים עתידיים של Claude
   יוכלו לתפעל את המסד ישירות (שחזור, שאילתות, ניהול משתמשים).

## אימות אחרי שחזור

- `https://countme-crm.vercel.app/api/health` (אחרי מיזוג ל-main) צריך להחזיר
  `{"ok":true,"db":"up"}`.
- התחברות עם Google עובדת ומגיעים ל-dashboard.
